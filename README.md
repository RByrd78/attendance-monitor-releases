# LMSA Attendance Dashboard — build host

Public host for the **signed/notarized macOS** and **Windows** builds of the LMSA
Attendance Professional Practice Dashboard, plus the auto-update manifest
(`latest.json`). **No source code lives here** — just the deliverable builds.

The app checks `latest.json` to offer in-app updates. Builds are attached to each
GitHub Release.
